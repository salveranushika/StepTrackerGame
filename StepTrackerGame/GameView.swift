import SwiftUI

struct GameView: View {
    var body: some View {
        Text("Game Screen - Unlocked on Goal Completion")
            .font(.largeTitle)
            .foregroundColor(.green)
    }
}
